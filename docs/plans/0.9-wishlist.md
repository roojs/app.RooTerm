# 0.9 — Wishlist

**Status:** ⏳ wishlist (open items only)

**Pointer:** `docs/guide-to-writing-plans.md` — **Checklist for plans**; Vala via **`docs/coding-standards-router.md`**. Build: **`docs/build-rules.md`**.

**ℹ️** Completed wishlist items → **`docs/plans/done/0.9.1-DONE-wishlist.md`**.
**ℹ️** Split plans: local restore **0.13**, tree expand **0.14**, colour select+preview **0.16**, fonts **0.17-DONE**, VTE menu **0.11**; further splits as needed.
**ℹ️** Renamed from **tidy-up** (2026-08-04) — general wishlist, not a single feature train.

---

## Purpose

- **🔷** Surface unfinished prefs / menus / shortcuts / connection / VTE chrome asks.
- **🚫** Deep split-pane / clusters / Ásbrú scripting — named only until we design them.

---

## Suggested order

1. **Full screen** — **`docs/plans/done/0.18-DONE-fullscreen.md`** — **Done**.
2. **Tree drag-and-drop** reorder — **secondary urgent**.
3. Scrollback length; scroll-to-bottom on keystroke (if missing).
4. Connection dialog layout + SSH options field.
5. Tab tooltip; Shell restart-with-confirm.
6. Edit group / LXC console·attach / clone.
7. Open link / quick open / search-web (+ search-engine pref); save VTE content.
8. **Search in terminal** (find UI + shortcut) — with VTE chrome.
9. Customize palette (low); prefer-dark / app theme — not urgent.
10. Startup login / SSH restore — not urgent.
11. **0.10** session history / VTE log (if not scheduled earlier).
12. Splits / clusters / drag-tabs — later.
13. LXC scan-then-tick; SSH path guessing; search→filter / opened vs full.

---

## Wishlist

### Urgency notes

- **🔷** **Full screen** — **`docs/plans/done/0.18-DONE-fullscreen.md`** — **Done**.
- **🔷** **Tree drag-and-drop** — **secondary urgent**.
- **🔷** **Blank-system install** — default **All** group covers first-host path; polish if import-empty UX still rough.
- **🚫** Customize palette / prefer-dark — **not urgent**.
- **🚫** Startup login / reconnect — **not urgent**.
- **ℹ️** Quit / reset shortcut wiring done; further keys land with their features (fullscreen, search-in-terminal).

---

### Host tree / search

#### Search → filter; opened vs full list

- **🔷** `⏳` Look at changing the host **search** into a **filter**.
- **🔷** `⏳` Think how the left side displays / toggles **currently opened** vs **full list** (or similar).

#### SSH path / cwd guessing

- **🔷** `⏳` Path guessing for the SSH shell is flaky.
- **🔷** `⏳` Investigate tightening the **prompt regex** and/or using **ANSI escape sequences** (e.g. OSC 7).

#### Clone connection

- **🔷** Right-click → clone connection.

#### Drag-and-drop reorder

- **🔷** Reorder hosts / groups in the tree by drag-and-drop — **secondary urgent**.

#### Groups (edit / delete)

- **🔷** **Edit group** — rename / edit a group (no edit path today; Edit connection is HOST-only).
- **ℹ️** **Delete group** — already in the tree menu when the group has no live children; keep / polish if edit lands.
- **ℹ️** **Blank install:** `Config.load` tries Ásbrú once when JSON missing; missing `asbru.yml` → catch → empty tree + Localhost. **Add connection** only on group right-click → polish if still rough.

#### Favorites / history

- **🔷** Favorites — interesting.
- **🔷** Connection history — possibly.
- **🚫** Ásbrú scripting — not wanted.
- **💩** Settings rollback / undo history — vague; design later if needed.

#### Clusters

- **🔷** Whole new ballgame — placeholder only.

---

### Connection / LXC

#### LXC host — scan then tick

- **🔷** `⏳` Today: tick “LXC host”, then scan / fetch — pointless order.
- **🔷** `⏳` Wanted: **Scan LXC hosts** button → if scan finds them, it **ticks**.
- **🔷** `⏳` Once ticked: can **re-scan**.
- **🔷** `⏳` Untick → control becomes the **Scan LXC hosts** button again (not tick-then-scan).

#### LXC container — console ↔ attach

- **🔷** `⏳` Right-click an LXC container: toggle between **attach** and **console**.
- **🔷** Container tree context menu: **console** / **attach**; `lxc_enter` default. Lifted from **0.4** Phase 19.
- **🚫** Edit connection on LXC rows (delete-only).

#### SSH options in Edit Connection

- **🔷** Plain “SSH options” text entry: load/save `connection.options`.
- **🔷** Useful toggles later: fold X11 (`-X` / `-x`), compression (default on?) — low urgency.
- **🚫** Forward agent, “allow remote port connection”, IP protocol — no clear need.
- **🚫** Remote port forwarding / dynamic SOCKS / Ásbrú “advanced” — skip unless asked.
- **🔷** Socks proxy / network settings — very low on the list.

#### Connection dialog layout

- **🔷** One row: connection method + host + port (tighter basic tab).

#### Pick private key

- **🔷** Choose a private key other than the ones we define / auto-create.

#### Lock down server (SSH keys only)

- **🔷** After key login works (follow-on to **Set up SSH key login**), offer **Lock down server (keys only)**.
- **🔷** Guided: verify key auth → show planned `sshd` changes (`PasswordAuthentication no`, etc.) → apply drop-in / config → reload → confirm reconnect still works.
- **🚫** Never silent / never default — easy to lock yourself out.
- **ℹ️** Keep sudo/password in keyring if elevation is still needed after password login is disabled.

#### Per-connection terminal options

- **🔷** Edit prompt/regex (etc.) per connection — interesting later.

#### Other protocols (Ásbrú parity — mostly out)

- **ℹ️** Ásbrú has FTP, RDP, SFTP, serial, Telnet, VNC, WebDAV…
- **🚫** Unlikely we support those; SSH (+ local / LXC) stays the scope unless asked.

#### Startup login / reconnect

- **🔷** **Not urgent.** Start the app and land back in sessions (login / reconnect) without re-opening everything by hand.

---

### Shell panel / app chrome

#### Restart from GNOME Shell

- **🔷** Restart option on the Shell panel menu, with confirmation.

#### Shell menu parity + room to grow

- **🔷** Panel menu should at least match actions we expose; grow with prefs.

#### Full screen

- **🔷** Full-screen mode — **Done** — **`docs/plans/done/0.18-DONE-fullscreen.md`**.

#### Tab tooltip (truncated names)

- **🔷** Tooltip on tab title when ellipsized (full name on hover).

#### Drag tabs to reorder

- **🔷** Low priority — nice if easy; not important soon.

---

### Appearance / VTE

#### App theme / prefer dark

- **🔷** Prefer dark theme (Adwaita / app).
- **🔷** Selecting themes for the app (beyond prefer-dark alone) — wishlist.

#### Fonts

- **✅** Font prefs page (family combo ←→ + size slider + preview) — **`docs/plans/done/0.17-DONE-font-preferences.md`**.

#### Colour schemes / palette

- **💩** Customize / edit palette (save as `… customized`) — **low priority**.
- **ℹ️** Select + preview shipped: **`docs/plans/done/0.16-DONE-color-theme-selection.md`** (also in **0.9.1**).

#### Scrollback

- **🔷** Configurable scrollback / history length.
- **🔷** Infinite scrollback — maybe; decide what that means later.

#### Scroll to bottom on keystroke

- **🔷** When the user types, jump the VTE back to the bottom (Ásbrú-style); check if we already do this — handy if not.

#### Search in terminal

- **🔷** VTE find UI (bar / dialog) + ``key_*`` + prefs row — feature first, shortcut with it.
- **ℹ️** Distinct from host search (``key-search`` / Ctrl+Shift+O).

---

### Keyboard shortcuts

- **ℹ️** Prefs page + registered keys → **0.9.1**; quit / reset → 2026-08-10.
- **🚫** No standalone “shortcut leftovers” queue — fullscreen / search-in-terminal keys ship with those features.
- **🔷** When bumping to **libadwaita ≥ 1.8**: show shortcuts with ``Adw.ShortcutLabel`` in ``RowKeySelect`` (keycaps instead of plain accelerator text). Needs 1.8+; we are on 1.7 / ``>= 1.5``.

| Action | Config key | Default |
| ------ | ---------- | ------- |
| Quit | `key-quit` | Ctrl+Shift+Q |
| Reset terminal | `key-reset-terminal` | Ctrl+Shift+K |

---

### Quick open (Ctrl-click)

- **🔷** Hover something that looks like a **file** or **URL** → Ctrl-click opens in editor / browser.
- **🔷** Tooltip hint: Ctrl-click to open.
- **🔷** Also from VTE context menu when selection / link fits.

---

### Open link (VTE context menu)

- **🔷** When selection / OSC is a URL → **Open link** on the VTE right-click menu.
- **ℹ️** Overlaps quick open; menu entry can share the same open path.

---

### Search on web

- **🔷** Select text → search on the web (context menu).
- **🔷** Optional: pick search engine (Google etc.) in prefs — interesting.

---

### Save VTE content

- **🔷** Save terminal content (interesting; design TBD — buffer vs visible, file picker).

---

### VTE context menu — follow-ons

- **ℹ️** v1 shipped: **`docs/plans/done/0.11-DONE-vte-context-menu.md`** (also in **0.9.1**).
- **🔷** Follow-ons: open link, search web, quick open, save content, split (placeholder), fullscreen (+ menu entry).

---

### Splits (section placeholder)

- **🔷** Horizontal / vertical splits and related shortcuts — whole new area; design later.
- **🔷** VTE context-menu **Split** entry waits on this.
- **🚫** Do not expand here until we pick a model.

---

## Attempts / changelog

- **ℹ️** Older done history moved to **`docs/plans/done/0.9.1-DONE-wishlist.md`**.
- **✔️** 2026-08-08 — split: open items stay here; completed → **0.9.1-DONE**; **0.16** customize left as low priority.
- **🔷** 2026-08-08 — urgency: fonts + shortcut leftovers **quite urgent**; tree DnD **secondary**; fullscreen **reasonably**; Suggested order moved to top.
- **🔷** 2026-08-09 — fonts → **`docs/plans/done/0.17-DONE-font-preferences.md`**.
- **🔷** 2026-08-10 — **0.17** UX: own Font page; family combo (System + monospace, in-face) + size slider; no font dialog.
- **✔️** 2026-08-10 — **0.17** implemented (agent); user confirms before DONE rename.
- **✅** 2026-08-10 — **0.17** DONE (`0.17-DONE-font-preferences.md`); user confirmed fonts working.
- **🔷** 2026-08-10 — ``Adw.ShortcutLabel`` for shortcut prefs rows when libadwaita ≥ 1.8.
- **✔️** 2026-08-10 — ``key-quit`` / ``key-reset-terminal`` accel wiring.
- **🔷** 2026-08-10 — drop “shortcut leftovers” as a queue; fullscreen / search-in-terminal are features first (keys with them).
- **🔷** 2026-08-10 — full screen → **`docs/plans/done/0.18-DONE-fullscreen.md`** (shipped).
