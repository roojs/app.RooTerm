# 0.11 — VTE right-click context menu

**Status:** DONE

**Pointer:** `docs/guide-to-writing-plans.md` — **Checklist for plans**; Vala via **`docs/coding-standards-router.md`**. Build: **`docs/build-rules.md`**.

**ℹ️** Right-hand pane = VTE. Right-click menu lives here (not drop-down chrome Quit). Shell panel still has Preferences / About / Quit.
**ℹ️** Tree context menu (console / attach, groups) stays under **0.9** / tree work — not this plan.
**ℹ️** Follow-ons (open link, search web, quick open, save content, split, fullscreen) → **`docs/plans/0.9-tidy-up.md`**.

---

## Purpose

- **🔷** Right-click on the terminal (VTE): useful context menu.
- **🔷** At least **Quit** (same as Shell panel / `DBus.quit`) when the user is not using the panel.
- **🔷** Grow to match a normal terminal + panel parity items.

---

## Menu items

### v1 (shipped)

- **🔷** `✅` {@link Terminal.ContextMenu} — owns {@link Gtk.PopoverMenu} + {@link GLib.Menu} / ``win.*`` (icons + accels); one on {@link MainWindow}; reparent; unmap / Toggle / Hide / destroy → `popdown`
- **🔷** `✅` Copy / Paste (`win.copy` / `win.paste`; Copy enabled only with selection)
- **🔷** `✅` Reset (`win.reset-terminal`)
- **🔷** `✅` Close (`win.close-terminal`)
- **🔷** `✅` Preferences / About / Quit (`win.preferences` / `win.about` / `win.quit`)
- **🔷** `✅` {@link Host.TreeMenu} — same PopoverMenu + model pattern (`tree.*` actions)

### Follow-ons → **0.9**

- **ℹ️** Open link, search on web, quick open, save content, split, toggle fullscreen — wishlist in **`docs/plans/0.9-tidy-up.md`** (not this plan).

---

## Already done elsewhere (0.3 Phase 17)

- **🔷** `✅` **About** dialog — Shell panel (`DBus.about`).
- **🔷** `✅` Host tree scroll-selected-into-view.

---

## Suggested order

1. **✅** Gesture + popover on VTE; Copy / Paste; Reset; Close; Preferences / About / Quit.
2. Follow-ons → **0.9**.

---

## Attempts / changelog

- **ℹ️** 2026-08-01 — lifted from 0.3 Phase 17 unfinished VTE menu; fuller item list from 0.9 wishlist.
- **ℹ️** 2026-08-02 — v1 batch: Copy/Paste, Reset, Close, Preferences/About/Quit. Fullscreen deferred (not supported yet).
- **ℹ️** 2026-08-02 — implemented `src/Terminal/ContextMenu.vala`; wired from {@link Terminal.Base}.
- **ℹ️** 2026-08-02 — fix: one menu on MainWindow (not per-tab); `popup_for` reparents like TreeMenu.
- **ℹ️** 2026-08-02 — both menus → PopoverMenu + GLib.Menu (drop button-box hack); TreeMenu same.
- **✅** 2026-08-02 — plan closed; leftovers → **0.9**.
