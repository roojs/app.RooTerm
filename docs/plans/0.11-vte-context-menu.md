# 0.11 — VTE right-click context menu

**Status:** ⏳ proposed (lifted from **0.3** Phase 17 + **0.9** wishlist)

**Pointer:** `docs/guide-to-writing-plans.md` — **Checklist for plans**; Vala via **`docs/coding-standards-router.md`**. Build: **`docs/build-rules.md`**.

**ℹ️** Right-hand pane = VTE. Right-click menu lives here (not drop-down chrome Quit). Shell panel still has Preferences / About / Quit.
**ℹ️** Tree context menu (console / attach, groups) stays under **0.9** / tree work — not this plan.

---

## Purpose

- **🔷** Right-click on the terminal (VTE): useful context menu.
- **🔷** At least **Quit** (same as Shell panel / `DBus.quit`) when the user is not using the panel.
- **🔷** Grow to match a normal terminal + panel parity items.

---

## Menu items

### v1 (thin)

- **🔷** Quit (→ `DBus.quit` / app quit path; honor quit-confirm from **0.9** when that lands)
- **🔷** Copy / Paste (shortcuts already exist)

### Next

- **🔷** Toggle fullscreen
- **🔷** Reset terminal (**want** — jammed sessions)
- **🔷** Close terminal
- **🔷** Open link (when selection / OSC is a URL)
- **🔷** Search on web (selection; optional search-engine pref in **0.9**)
- **🔷** Quick open (file / URL — see **0.9**)
- **🔷** Preferences / About / Quit (same as GNOME Shell panel menu)
- **🔷** Save content — interesting
- **🔷** Split — later (**0.9** splits placeholder)

---

## Already done elsewhere (0.3 Phase 17)

- **🔷** `✔️` **About** dialog — Shell panel (`DBus.about`).
- **🔷** `✔️` Host tree scroll-selected-into-view.

---

## Suggested order

1. Gesture + popover on VTE; Quit + Copy + Paste.
2. Reset / close / fullscreen.
3. Open link / search web / quick open.
4. Preferences / About entries; save content if wanted.

---

## Attempts / changelog

- **ℹ️** 2026-08-01 — lifted from 0.3 Phase 17 unfinished VTE menu; fuller item list from 0.9 wishlist.
