# 0.9.1 — DONE wishlist archive

**Status:** DONE (archive of wishlist items shipped outside / via split plans)

**Pointer:** Open work stays in **`docs/plans/0.9-wishlist.md`**. This file is history only.

**ℹ️** Split from **0.9** (2026-08-08) so the live wishlist lists only unfinished items.
**ℹ️** Items here may have been done inline, via bugs, or via numbered plans (0.11 / 0.13 / 0.14 / 0.16, …).

---

## Purpose

- **🔷** Keep a readable archive of wishlist asks that are already done.
- **🔷** Point at the plan / bug that closed each item when there is one.

---

## Done (from wishlist)

### Tabs / shortcuts

#### Double-click empty tab bar

- **🔷** `✅` Empty region of the tab bar: **double-click** → new terminal (`Host.TabBar` / `win.new-terminal`).

#### Background tab close — animate + cancel

- **🔷** `✅` Closing any tab (selected or background):
  - Keep the **normal title**.
  - **X** → **pause** (cancel); cancel restores X.
  - Fill: **one** orange Overlay child; idle = `visible = false`, `width_request = 1`; closing = visible + width from chip `row.get_width()` × countdown.
  - **Active:** also VTE `close_bar`. **Background:** chip underlay only.
- **ℹ️** Width feedback bug: `docs/bugs/done/2026-08-08-FIXED-tabbar-width-feedback-loop.md`.

#### New-tab shortcuts (split by kind)

- **🔷** `✔️` **Ctrl+Shift+T** / tab **`+`** → always new **terminal** (`win.new-terminal`).
- **🔷** `✔️` **Ctrl+Shift+S** → new **SSH** for focused host (`win.new-ssh`); on Localhost same as **T**.

---

### Host tree / connection persistence

#### Groups — add group

- **🔷** `✔️` **Add group** — blank-space context menu (name dialog); default **All** if none; new hosts only via group menu (no migration of legacy top-level).

#### Remember tree expand / collapse

- **🔷** `✔️` **`docs/plans/done/0.14-DONE-tree-expand-save-restore.md`**.

#### Auto-restore open tabs

- **🔷** `✔️` **`docs/plans/done/0.13-DONE-auto-restore-local-tabs.md`**.

#### Find / open hosts

- **🔷** `✅` **`docs/plans/done/0.19-DONE-find-open-hosts.md`**.

#### Last tab per workspace

- **🔷** `✅` **`docs/plans/done/0.20-DONE-workspace-last-tab.md`**.

#### Quit confirmation

- **🔷** `✔️` Confirm before quit when terminals are still open (`DBus.quit` → `Adw.AlertDialog`; Shell / VTE / ``--quit`` / normal-mode window close).

---

### Appearance / VTE

#### Colour schemes / palette (select + preview)

- **🔷** `✔️` **`docs/plans/done/0.16-DONE-color-theme-selection.md`** — catalogue, prefs selectors, live preview.
- **ℹ️** Customize / edit palette stays open on **0.9** (low priority).

---

### Keyboard shortcuts (prefs page — initial set)

- **🔷** `✔️` Prefs sidebar page **Keyboard shortcuts**; Config ``key_*``; live ``config_update`` + accels.
- **🔷** Registered: toggle, search, new terminal, new SSH, close terminal, prev/next tab, select all, copy, paste, preferences.

| Action | Config key | Default |
| ------ | ---------- | ------- |
| Toggle visibility | `key-toggle` | F12 |
| Search hosts | `key-search` | Ctrl+Shift+O |
| New terminal | `key-new-terminal` | Ctrl+Shift+T |
| New SSH | `key-new-ssh` | Ctrl+Shift+S |
| Close terminal | `key-close-terminal` | Ctrl+Shift+W |
| Previous / next tab | `key-prev-tab` / `key-next-tab` | Ctrl+Shift+Left / Right |
| Select all | `key-select-all` | Ctrl+Shift+A |
| Copy / Paste | `key-copy` / `key-paste` | Ctrl+Shift+C / V |
| Preferences | `key-preferences` | Ctrl+, |

- **ℹ️** Quit / reset / fullscreen / search-in-terminal etc. still open on **0.9**.

---

### VTE context menu (right-click) — v1

- **🔷** `✔️` **`docs/plans/done/0.11-DONE-vte-context-menu.md`** — Copy / Paste, Reset, Close, Preferences / About / Quit; PopoverMenu + `win.*` / TreeMenu same pattern.
- **ℹ️** Follow-ons (open link, search web, quick open, save, split, fullscreen menu) stay on **0.9**.

---

### Done outside numbered wishlist plans

- **✔️** Shell panel menu order: **Preferences → About → Quit**.
- **✔️** Edit Connection → standalone `Adw.Window` + Shell keep-above + centre on open; later **0.12** (`docs/bugs/done/2026-08-01-FIXED-edit-connection-lost-on-hide.md`).

---

## Attempts / changelog

- **ℹ️** 2026-08-08 — created as **0.9.1**: moved completed **0.9** wishlist items here so **0.9** is open work only.
- **✔️** 2026-08-08 — includes **0.16-DONE** colour select + preview (customize left on **0.9**).
