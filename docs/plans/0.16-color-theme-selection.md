# 0.16 — Colour theme selection

**Status:** ⏳ proposed / not started

**Pointer:** `docs/guide-to-writing-plans.md` — **Checklist for plans**; Vala via **`docs/coding-standards-router.md`**. Build: **`docs/build-rules.md`**.

**ℹ️** Split from **`docs/plans/0.9-wishlist.md`** Appearance → Colour schemes / palette.
**ℹ️** Today: hard-coded `/solarized-dark.json` in `Terminal/Base`; prefs Appearance has opacity / geometry only.

---

## Purpose

- **🔷** Pick a VTE colour theme in preferences, with a real preview (better than Guake’s).
- **🔷** Phase 1: precursor — Guake → five keyfiles, theme objects / list store, config, apply in terminals.
- **🔷** Phase 1.5: preferences **selectors** — Background colour + Foreground theme (← | combo | →).
- **🔷** Phase 1.6: preferences **theme preview** (live VTE + opacity glass).
- **🔷** Phase 2: customize a theme and save as `… customized`.
- **ℹ️** VTE palette only — app Adwaita / prefer-dark stays on the wishlist.

---

## Phase 1 — Precursor (catalogue + apply)

### Catalogue (Guake → keyfiles)

- **🔷** Take Guake’s full **~169** as the stock set.
- **🔷** Convert once into **GKeyFile**s in a **gresource**; vendor in-tree (no runtime Guake / Python).
- **🔷** **Five** keyfiles — **one per background category**, named:
  - `theme-black.ini`
  - `theme-dark-grey.ini`
  - `theme-dark.ini`
  - `theme-off-white.ini`
  - `theme-white.ini`
- **🔷** Load all five into the list store in one go at startup / catalogue load.
- **🔷** Group = theme name; keys `foreground`, `background`, `palette` (16 colours); `#RRGGBB`.
- **🔷** When importing names: **strip Dark / Light** (and similar); tidy (e.g. `Solarized Dark` → `Solarized`).
- **🔷** Same tidy name **OK in different category files** (dark `Solarized` vs light `Solarized`) — that is why we split by file.
- **🔷** Attribute the colour data to Guake (fine).
- **ℹ️** Guake `palettes.py`: 16 palette + fg + bg each.
- **ℹ️** Ásbrú has per-colour YAML, not a scheme catalogue.

```ini
; e.g. resources/themes/theme-dark.ini
[Solarized]
foreground=#708183
background=#001E26
palette=#002831;#d30102;...
```

### Background categories (for convert)

- **🔷** Classify from **background** luminance ± chroma (not name tokens).
- **🔷** Labels / files: **Black**, **Dark grey**, **Dark**, **Off-white**, **White**.
- **🔷** Cream + tiny mid-grey set → **Off-white**.
- **🔷** Math:
  - `L` = relative luminance of background
  - `chroma` ≈ max(RGB) − min(RGB)
  - black / dark grey / dark from low `L` + chroma cut
  - off-white / white from high `L`

### Theme objects + list models

- **🔷** Load all five keyfiles → {@link Theme} rows in {@link Themes} (top-level `RooTerm`, not `Terminal`).
- **🔷** `Themes` is `GLib.ListModel` over ArrayList (TreeNodes pattern); owned as `Config.themes`; `by_key` / `by_category`.
- **ℹ️** Prefs selectors (1.5) use `config.themes.by_category` for the filtered theme list.

### Config + apply to terminals

- **🔷** Persist the selected theme on config — **name + category** (names are only unique inside a category file).
- **🔷** Replace hard-coded Solarized load in `Terminal/Base` with the **selected** theme (fg / bg / palette from the catalogue).
- **🔷** Theme changes apply to open terminals live (like opacity `notify`) — prefs selectors (1.5) drive this.
- **🔷** Missing / unknown selection → **first theme** in **Black** — keep it simple.
- **🔷** No migration from today’s hard-coded Solarized — user picks a theme when this ships.

### Phase 1 tasks

- **🔷** `✔️` Convert Guake ~169 → five `theme-*.ini` in gresource; tidy names; attribute Guake.
- **🔷** `✔️` `Theme` row; `Config.themes` (`Themes` ListModel + maps).
- **🔷** `✔️` Config theme name + category; `Terminal/Base` loads selected theme (drop hard-coded path).
- **🔷** `✔️` Live apply when config theme changes (open tabs).

---

## Phase 1.5 — Preferences selectors

### Rows

- **🔷** Two rows under Appearance (preview is Phase 1.6):
  - **Background colour** — reuse `Dialog.RowCombo` (default **Black**); drives the filtered theme list.
  - **Foreground theme** — new theme-selector row: **←** | **combo** | **→**.
- **🔷** **Background colour** selects `config.themes.by_category` ListStore for that bucket.
- **🔷** ← / → cycle the **filtered** list without opening the combo (combo still for jump-to-name).
- **🔷** Open combo must not be the only browse path — when preview lands (1.6) it would cover it; cycling still useful before that.
- **🔷** Wire selector changes to config / live terminals (Phase 1 apply path).

### Phase 1.5 tasks

- **🔷** `⏳` `RowCombo` Background colour (default Black) + theme combo on `config.themes.by_category`.
- **🔷** `⏳` Foreground theme row: ← | combo | →.
- **🔷** `⏳` Wire prefs changes to config / live terminals.

---

## Phase 1.6 — Theme preview

### Preview VTE

- **🔷** New **Theme preview** row under the selectors.
- **🔷** Live VTE, ~10 lines, `sensitive = false` (not editable).
- **🔷** 16 colours; a few coloured example words per line.
- **🔷** Loose line shape: often a command then short words — not two command columns.
- **🔷** Prompt line **first** (critical). Sketch approved:

```
user@host:~/src$ _
ls   directory  symlink  executable  socket
git  add  delete  commit  branch
gcc  error  warning  note
…  dim  emphasis  …
```

- **ℹ️** Word colours follow common `ls` / `git` / `gcc` roles (directory, symlink, error, …).
- **ℹ️** Guake’s own prefs preview is terrible — ours is the discovery path with ← / → (1.5).

### Transparency on preview

- **🔷** Preview follows `config.opacity`.
- **🔷** Prefs chrome is opaque — need a stand-in “desktop” behind the translucent VTE.
- **🔷** Use the **desktop background image** (wallpaper), crop the **middle** of it, and put that behind the preview.
- **ℹ️** On GNOME, wallpaper path is typically `org.gnome.desktop.background` `picture-uri` / `picture-uri-dark` (gsettings).
- **🔷** No usable wallpaper image → solid fallback (muted / off-blue).

### Phase 1.6 tasks

- **🔷** `⏳` Theme preview row: live VTE, sketch above, `sensitive = false`.
- **🔷** `⏳` Wallpaper middle-crop (or off-blue) behind preview for opacity.
- **🔷** `⏳` Preview follows theme / opacity changes from selectors.

---

## Phase 2 — Customize

### Behaviour

- **🔷** Start from the current theme; edit colours.
- **🔷** Save as base name + ` customized` (e.g. `Solarized customized`).
- **🔷** Still open: one customized slot per base name vs many “Save as…” names.
  - **💩** One slot (overwrite) — simpler.
  - **💩** Free Save-as — more UI, later.

### UI

- **💩** Colour buttons for fg, bg, and 16 palette slots.
- **💩** Editing switches selection to the `… customized` theme (or creates it).
- **💩** Reset drops custom data and re-selects stock.

### Storage

- **💩** Stock stays read-only in gresource; customized under user config (same keyfile shape if practical).

### Phase 2 tasks

- **💩** `⏳` Pick one `… customized` overwrite vs Save-as many.
- **🔷** `⏳` Colour editors + write custom theme (once storage rule is picked).
- **🔷** `⏳` Combo lists stock + customized.
- **🔷** `⏳` Preview / open terminals follow customized colours.

---

## LLM notes

- **🔷** No runtime Guake / Ásbrú for the catalogue (vendor data; attribute Guake).
- **🚫** App-wide Adwaita theme / prefer-dark (wishlist).
- **🚫** Per-connection colour overrides unless asked.
- **ℹ️** Config stores category with the theme name so lookup stays unique across the five files; Background colour row (1.5) filters the list; preview is 1.6.

---

## Attempts / changelog

- **ℹ️** 2026-08-08 — drafted from chat (Guake catalogue, keyfile, categories, ←combo→, live preview, wallpaper opacity, Phase 2 customize).
- **ℹ️** 2026-08-08 — split former Phase 1 into **Phase 1** (precursor) + **Phase 1.5** (preferences UI).
- **✔️** 2026-08-08 — Phase 1: five `theme-*.ini`, `Theme`/`Themes` (RooTerm), Config theme fields, Base apply + notify.
- **ℹ️** 2026-08-08 — split 1.5 into **1.5 selectors** + **1.6 theme preview**.
